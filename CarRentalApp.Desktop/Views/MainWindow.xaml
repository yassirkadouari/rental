<Window x:Class="CarRentalApp.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CarRentalApp.Desktop.Views"
        xmlns:vm="clr-namespace:CarRentalApp.Desktop.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        mc:Ignorable="d"
        Title="Car Rental Admin - Dashboard" Height="700" Width="1000" 
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style TargetType="Button" x:Key="NavButtonStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                   <Setter Property="Background" Value="#444"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#2D2D30">
            <StackPanel>
                <TextBlock Text="Car Rental" Foreground="White" FontSize="22" FontWeight="Bold" Margin="20,20,20,40"/>
                
                <Button Content="Dashboard" Command="{Binding NavigateToHomeCommand}" Style="{StaticResource NavButtonStyle}"/>
                <Button Content="User Management" Command="{Binding NavigateToUsersCommand}" Style="{StaticResource NavButtonStyle}"/>
                <Button Content="Vehicles" Command="{Binding NavigateToVehiclesCommand}" Style="{StaticResource NavButtonStyle}"/>
                <Button Content="Rentals" Command="{Binding NavigateToRentalsCommand}" Style="{StaticResource NavButtonStyle}"/>
                <Button Content="Reports" Style="{StaticResource NavButtonStyle}"/>
            </StackPanel>
        </Border>
        
        <!-- Content -->
        <Border Grid.Column="1" Background="#F0F0F0">
            <ContentControl Content="{Binding CurrentView}" Margin="20">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type vm:UsersViewModel}">
                        <local:UsersView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vm:VehiclesViewModel}">
                        <local:VehiclesView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vm:RentalsViewModel}">
                        <local:RentalsView/>
                    </DataTemplate>
                </ContentControl.Resources>
                
                <!-- If CurrentView is null or string, fallback? -->
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentView}" Value="{x:Null}">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <Grid>
                                                <TextBlock Text="{Binding DataContext.WelcomeMessage, RelativeSource={RelativeSource AncestorType=Window}}" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#555"/>
                                            </Grid>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>
        </Border>
    </Grid>
</Window>
